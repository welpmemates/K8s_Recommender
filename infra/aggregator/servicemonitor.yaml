apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: aggregator-monitor
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: aggregator
  endpoints:
    - port: http
      path: /metrics
      interval: 5s
