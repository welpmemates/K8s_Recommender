apiVersion: v1
kind: Service
metadata:
  name: mock-app
  labels:
    app: mock-app
spec:
  type: NodePort
  selector:
    app: mock-app
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30007
