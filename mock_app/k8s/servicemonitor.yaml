apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: mock-app-monitor
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: mock-app
  endpoints:
    - port: http
      path: /metrics
      interval: 5s