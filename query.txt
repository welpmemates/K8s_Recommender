irate(app_requests_total[15s])

sum(rate(container_cpu_usage_seconds_total{pod=~"mock-app.*"}[30s]))

sum(container_memory_usage_bytes{pod=~"mock-app.*"})

predicted_cpu_usage

predicted_memory_usage

safe_cpu_usage

safe_memory_usage

cpu_mae

cpu_rmse

cpu_spike_miss
